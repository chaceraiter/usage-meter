# AI context management — private working notes, not for public repo
ai-context-management/

# Secrets / local config
.env
.env.local
*.pem
*.key

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Rust / Tauri
target/
**/*.rs.bk
src-tauri/target/
# NOTE: src-tauri/Cargo.lock IS committed — usage-meter ships as a
# binary, so a locked dependency graph is what users actually build
# against and what dependabot needs to update.

# Node / frontend
node_modules/
dist/
dist-ssr/
build/
.parcel-cache/
.vite/

# Logs
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Coverage
coverage/
*.lcov

# Tauri build artifacts
src-tauri/gen/
src-tauri/WixTools/

# Spike outputs (may contain sensitive network data / cookies)
spike/output/
spike/node_modules/
spike/pnpm-lock.yaml
spike/package-lock.json
